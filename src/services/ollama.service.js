/**
 * Forwarding bridge to Google Gemini AI Engine
 * Maintains backwards compatibility for all existing routes
 */

const geminiService = require('./gemini.service');

module.exports = geminiService;
